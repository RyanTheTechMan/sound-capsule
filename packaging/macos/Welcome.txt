Sound Capsule installs the standalone application in /Applications and configures
the FL Studio MIDI bridge for the current user.

The local helper is self-contained. No Python or uv installation is required.
If setup cannot finish during installation, first launch retries it automatically.
